#include <stdio.h>

int gr(int a, int b){
    
}

int main(){
    int a,b;
    printf("Enter both number = ");
    scanf("%d%d",&a,&b);
    printf("Greatest divisor of %d and %d = %d\n",a,b,gr(a,b));
    return 0;
}