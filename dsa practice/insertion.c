#include <stdio.h>

int main(){
    int a,key,j;
    printf("Enter element = ");
    scanf("%d",&a);
    int b[a];
    printf("Enter %d element = ",a);
    for (int i = 0; i < a; i++)
    {
        scanf("%d",&b[i]);
    }
    for (int i = 0; i < a; i++)
    {
        key = b[i];
        
        while (j>=0 && )
        {
            /* code */
        }
        
    }
    
}